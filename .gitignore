node_modules/
cache/
artifacts/
coverage/
/coverage.json
typechain/
typechain-types/
.eslintcache
reports/

# Marp deck previews (regenerate with `npm run deck:build` / `npm run results:build`).
# The final docs/pd3/pitch-deck.pdf submission artifact is NOT ignored — commit that one.
docs/pd3/*.html

.env
.env.*
!.env.example

.DS_Store
.idea/
.vscode/
.claude/
*.log
sources/